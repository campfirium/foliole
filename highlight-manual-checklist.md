# 高亮与挖空人工验收清单

## 说明

这份清单按最近一组高亮迁移提交整理，覆盖当前新模型下需要人工过一遍的功能。

建议验收顺序：

1. 先验正文内文本高亮与文本挖空。
2. 再验节点导航、侧边栏、沉浸式阅读。
3. 最后验删除、恢复、重开、跨页面返回这些容易漏掉的链路。

---

## 1. 文本高亮创建

- [ ] 在普通正文里选中一段文字，右键创建高亮，能成功生成子节点。
- [ ] 创建高亮后，当前仍停留在原节点，不会被强制跳走。
- [ ] 新建的高亮子节点标题正确。
- [ ] 原文中的高亮渲染位置正确，没有偏一位、少一位、多一位。
- [ ] 同一父节点下连续创建多个高亮，都能正常出现。
- [ ] 相邻高亮都能显示，不会互相吃掉。
- [ ] 有重叠高亮时，显示仍连续，不会断裂或白块。

## 2. 文本挖空创建

- [ ] 在普通正文里选中一段文字，右键创建挖空，能成功生成子节点。
- [ ] 挖空子节点的题面正确显示 `[...]`。
- [ ] 挖空子节点的答案正确。
- [ ] 创建挖空后，当前仍停留在原节点，不会跳走。
- [ ] 同一父节点下连续创建多个挖空，都能正常出现。
- [ ] 一次选中多段文字创建挖空时，多个空都能保留下来，不会只剩第一个。
- [ ] 多段挖空重新打开后仍完整，不会丢段。

## 3. 右键与选区稳定性

- [ ] 先选中文本，再右键，菜单打开后选区仍然还在。
- [ ] 右键后立即点“高亮”或“挖空”，作用到的就是刚才那段文字。
- [ ] 连续重复做“选中 -> 右键 -> 高亮/挖空”时，不会出现错绑。
- [ ] 在高亮前面输入文字时，高亮不会抖动。
- [ ] 在高亮内部输入文字时，高亮仍稳定跟随。
- [ ] 在高亮边界输入或删除文字时，不会出现闪烁、跳位或范围错乱。

## 4. 编辑后跟随

- [ ] 父节点正文在高亮前面插字后，高亮还能跟到原来的文字。
- [ ] 父节点正文在高亮内部改字后，高亮子节点内容会同步更新。
- [ ] 父节点正文在挖空答案处改字后，挖空子节点答案会同步更新。
- [ ] 多段挖空中只改其中一段时，题面和答案仍正确。
- [ ] 删除整段被标注文字后，不会直接把页面搞坏。
- [ ] 被删空的标注再次回到父节点时，不会白屏。

## 5. 节点与面包屑导航

- [ ] 从高亮子节点点击面包屑回到父节点，页面正常显示。
- [ ] 从挖空子节点点击面包屑回到父节点，页面正常显示。
- [ ] 回到父节点后，原文中的对应高亮还能看到。
- [ ] 从侧边栏点某个高亮项，能正确定位回原文。
- [ ] 从子节点返回父节点时，树形结构不会被错误掰平。

## 6. 侧边栏高亮列表

- [ ] 右侧 Highlights 面板能列出当前节点子树中的文本高亮。
- [ ] 文本挖空也会出现在 Highlights 面板里。
- [ ] 列表里的类型标签能区分 Highlight 和 Cloze。
- [ ] 点列表项会跳到正确的父节点与正确位置。
- [ ] 已失效但仍保留的文本标注，在列表里仍可见。

## 7. 沉浸式阅读

- [ ] 在沉浸式阅读里选中文字创建高亮，结果正确。
- [ ] 在沉浸式阅读里选中文字创建挖空，结果正确。
- [ ] 沉浸式阅读里切换节点后，再做高亮/挖空不会串到旧节点。
- [ ] 沉浸式阅读里切换显示状态后，高亮开关仍稳定。

## 8. 删除、恢复与清理

- [ ] 删除高亮子节点后，原文中的对应高亮会一起消失。
- [ ] 删除挖空子节点后，原文中的对应挖空会一起消失。
- [ ] 恢复被删的高亮子节点后，原文中的对应高亮会回来。
- [ ] 恢复被删的挖空子节点后，原文中的对应挖空会回来。
- [ ] 永久删除后，不会残留幽灵高亮或幽灵挖空。

## 9. 重开与持久化

- [ ] 创建高亮后关闭再打开应用，高亮仍在。
- [ ] 创建挖空后关闭再打开应用，挖空仍在。
- [ ] 多段挖空关闭再打开后仍完整。
- [ ] 重开后从高亮子节点回到父节点，不会白屏。
- [ ] 重开后侧边栏中的高亮列表仍正确。

## 10. 复制、粘贴与外部来源

- [ ] 应用内复制带高亮的文本，再粘贴回正文，不会把页面搞坏。
- [ ] 外部导入的新高亮节点仍能在侧边栏与原文里对上。
- [ ] 已导入的高亮文本在节点更新后，仍能继续对位。

## 11. 图片挖空

- [ ] 图片挖空区域可以创建。
- [ ] 同一张图上可以连续创建多个区域。
- [ ] 删除某个图片区块时，只删除对应区域，不误删别的。
- [ ] 从图片挖空子节点回到来源节点，图片区域仍正常显示。

## 12. 基本回归观察项

- [ ] 任意高亮或挖空创建后，没有明显卡顿。
- [ ] 连续创建多个标注后，没有白屏。
- [ ] 切换节点、切换回来后，没有整段消失。
- [ ] 控制台没有持续报错。

---

## 最近这组提交主要覆盖的点

- `000612`：沉浸式阅读里的高亮开关与选择链路。
- `000613`：文本高亮从旧结构切到新定位模型，接通显示、同步、跳转、落库。
- `000614`：右侧高亮列表补齐文本高亮、挖空和失效项显示。
- `000615`：补强一批冒烟测试，防止主链回归。
- `000616`：修正文前输入时高亮抖动。
- `000617`：修复从高亮子节点经面包屑回父节点时白屏。
- `000618`：修复多段文本挖空只保存第一段的问题。
