# Foliole 设计风格指南

这份文档定义了 Foliole 在本仓库中应该呈现出的界面气质与设计规则，供 AI 和人工在做界面时共同遵循。

它是一份面向产品界面的长期设计指南，适用于桌面优先的工作台应用。它不是营销页参考，也不是通用后台模板说明。

Foliole 应该给人的感觉是安静、专注、精确、耐用，适合长时间阅读、写作、整理和复习，而不会造成视觉疲劳。

## 1. 整体气质与氛围

### 核心个性

Foliole 是一个带有学习闭环的桌面知识工作台。

它的视觉个性应该是：

- 安静，而不是喧闹
- 精确，而不是装饰感很强
- 温和中性，而不是冰冷或刻意高级
- 有结构，而不是轻浮活泼
- 在工作区域里可以高密度，但不能拥挤
- 适合长期高频使用

### 产品氛围

整个界面应该像一个认真做思考、编辑和整理工作的桌面工具，而不是：

- 创业公司官网
- 光鲜的 SaaS 后台
- 创意作品集网站
- 到处是渐变和光效的“AI 产品”

它的情绪目标应该是：

- 写作时足够安静
- 导航时足够有秩序
- 高频操作时足够利落

### 桌面优先姿态

Foliole 首先是桌面工作区，不是把网页硬装进窗口。

主壳应该让人感受到：

- 这是一个完整应用窗口
- 这是一个稳定工作台
- 这是一个持续的编辑环境

而不是：

- 一堆彼此抢注意力的卡片
- 很多浮在页面上的宣传式大块
- 留白过度、像网站而不像工具的布局

## 2. 颜色体系与角色

### 颜色方向

默认基调是中性灰白。

绿色不是整套界面的主气氛色，它只是一个克制使用的强调色，用来表达重点、进度、确认或少量产品识别。

### 主色盘

以下数值以当前仓库已有基线为准，除非后续明确改版，否则默认继续作为基础真相使用。

| 角色 | 数值 | 用途 |
|---|---|---|
| `canvas` | `#ffffff` | 窗口级白色表面、正文内容背景 |
| `background` | `#f5f5f3` | 应用整体背景 |
| `bg-subtle` | `#fcfcfc` | 很轻的悬停态或辅助表面 |
| `bg-panel` | `#f6f6f6` | 侧栏、检查器、工具型面板背景 |
| `bg-elevated` | `#ffffff` | 浮层、对话框、聚焦表面 |
| `foreground` | `#202124` | 主要文字和图标颜色 |
| `border` | `rgba(32, 33, 36, 0.18)` | 默认分隔线和控件边框 |
| `border-strong` | `rgba(32, 33, 36, 0.34)` | 更强的边界和选中描边 |
| `accent` | `#5f6368` | 中性动作强调 |
| `accent-strong` | `#3c4043` | 更强的中性强调 |
| `green-accent` | `#3f8f68` | 少量品牌/进度/确认强调 |
| `selection-blue` | `#3876ff` | 仅用于选中态 |
| `highlight-blue` | `#38bdf8` | 仅用于高亮覆盖层 |
| `cloze-yellow` | `#facc15` | 仅用于挖空或学习标注 |

### 用色规则

- 大多数界面都应停留在中性颜色范围内。
- 结构层次应优先通过分隔线、间距和表面深浅变化来表达，而不是依赖大面积强色。
- Foliole 是多区域、多 surface 的桌面工作台；写任何区域内颜色时，必须先确认当前元素属于哪个 workspace region / surface。
- 区域内的 hover、active、selected、scrollbar、divider、control background、panel background 等派生色，默认都应以当前区域底色为母色做轻微明度或透明度变化。
- 不要默认用全局灰、全局 foreground 透明度或相邻区域颜色来生成局部颜色；只有文字、图标、焦点环和明确边界线等跨 surface 语义元素，才优先消费全局 foreground / border token。
- 当两个区域底色不同，局部控件必须贴合自己所在区域，而不是为了“统一”被压成同一种灰。
- 绿色应少量使用，只在正向反馈、产品重点或明确确认动作上出现。
- 蓝色和黄色是功能色，不是主题色。
- 红、橙、绿等语义色可以用于状态标签和警告，但应控制在明确的状态场景里。

### 颜色不该做什么

不要拿颜色去弥补结构不清。

避免：

- 大块主色区域撑气氛
- 只是为了好看而整面板染色
- 界面同时出现太多颜色中心
- 把渐变当默认语言
- 整体走偏紫、偏霓虹的气质

## 3. 字体与文字层级

### 文字气质

Foliole 的文字应当可读、稳定、克制。

它需要有一点偏编辑工具的阅读感，但不能太文艺；也需要有一点工具感，但不能生硬得像纯工程软件。

### 字体家族

- 界面默认字体栈：`-apple-system, BlinkMacSystemFont, 'SF Pro Text', 'Segoe UI Variable', 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei UI', 'Microsoft YaHei', 'Noto Sans CJK SC', 'Noto Sans SC', sans-serif`
- 正文默认字体栈：`-apple-system, BlinkMacSystemFont, 'SF Pro Text', 'Segoe UI Variable', 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei UI', 'Microsoft YaHei', 'Noto Sans CJK SC', 'Noto Sans SC', sans-serif`
- 等宽默认字体栈：`'JetBrains Mono', 'Cascadia Code', 'Sarasa Mono SC', 'SFMono-Regular', Menlo, Consolas, 'Noto Sans Mono CJK SC', monospace`

不要额外引入存在感很强的品牌字体作为默认主字体。

当前设置系统允许切换的常见界面字体预设包括：

- `default/system`
- `Inter`
- `Source Sans 3`
- `Source Serif 4`
- `SF Pro Rounded`

但如果没有明确产品决策，默认仍以系统无衬线栈为基线。

### 层级原则

- 窗口 chrome 和元信息应紧凑、克制
- 分组标题应清楚，但不要过度放大
- 面板标题应稳，不要像营销页标题
- 正文标题可以更有层次，但仍应保持安静
- 说明文字要可读，但不能抢主体

### 默认字号基线

以下是当前最适合 AI 直接落地的默认字号建议：

| 场景 | 默认字号 | 说明 |
|---|---|---|
| 窗口元信息、微型标签 | 11px | 仅用于时间戳、窄元信息、极小结构标签 |
| 辅助标签、小说明 | 12px | 次要状态、辅助提示、弱化信息 |
| 普通正文与大多数控件标签 | 14px-15px | 常规界面文本首选 |
| 输入框正文 | 16px | 当前 `AppInput` 基线 |
| section 标题 / 面板标题 | 14px-16px | 要稳，不要夸张 |
| 对话框标题 | 18px 左右 | 只用于浮层主标题 |
| 正文编辑默认字号 | 17px | 当前编辑区基础字号 |

界面默认基准字号按 15px 理解，正文编辑默认基准字号按 17px 理解。

### 文字行为

- 大多数标签优先使用正常句式，不要全大写
- 只有在极小的结构标签、窄标题条、辅助元信息里才适合用全大写
- 长时间阅读界面不应使用过紧的字距或过轻的字重
- 导航区允许适度截断，但层级关系必须依然清楚

### 阅读与编辑区

编辑区应明确服务于持续阅读：

- 行高要稳定
- 标题层级要清楚
- 正文字号要舒服
- 等宽字体只在语义明确时使用

代码感不应该蔓延到普通内容区。

当前编辑区字号比例可按下面这组默认值理解：

| 层级 | 默认值 |
|---|---|
| 正文 | 17px |
| H1 | 24.14px |
| H2 | 20.06px |
| H3 | 17.68px |
| 行内/块级代码 | 15.3px |

以上比例来自当前设置模型，后续如需调整，应整体改比例，不要单改某一级。

## 4. 常见界面块风格

### 共享组件基线

当前共享组件已经构成了 Foliole 的基础界面语言，后续新增界面应优先建立在这套基线之上：

- `AppButton`
- `AppIconButton`
- `AppInput`
- `AppPanel`
- `AppToolbar`
- `ToolbarActionGroup`
- `AppDropdownMenu*`
- `AppDialog*`
- `AppTooltip*`
- `SettingsSection`
- `SettingsRow`
- `SettingsControlSlot`
- `AppBreadcrumb`
- `AppEmptyState`
- `AppStatusBadge`
- `ReviewActionBar`
- `InspectorSection`

如果新功能需要一个常见控件，优先扩展这套体系，而不是另写一套局部外观。

### 滚动条

Workspace 内部滚动条应当是低存在感的操作反馈，而不是结构线或强调元素。

- 默认不显示高对比轨道，track 应保持透明或近透明。
- thumb 以当前滚动区域的底色为基准，只做轻微明度差；可以保留当前区域的细微色相，但不跟随品牌色、状态色或相邻面板的强色相。
- 静止状态保持安静，hover、滚动中或拖动时才允许轻微提高可见度。
- 滚动条不得形成贯穿面板的视觉边框；面板分隔应由 divider、背景层级和布局完成。
- 长列表中仍应保留可发现的滚动位置反馈，不应为了安静而完全隐藏主要导航滚动条。
- 桌面工作区默认采用窄型 scrollbar：视觉 thumb 要细，命中区域可以略宽。
- 左侧树、内容列表、编辑区和右侧检查器应共享同一套 scrollbar token，不在局部面板里各自定义颜色体系。

### 按钮

按钮应该紧凑、克制、偏桌面工具感。

- 默认动作应轻，不要太“满”
- 悬停反馈要细，不要大起大落
- 圆角要柔和，但不能软塌塌
- 强主按钮应少用
- 列表式动作应像结构项，不要像大胶囊按钮

当前共享按钮可直接按这组默认规格理解：

| 组件 | 高度 | 横向内边距 | 圆角 | 边框 |
|---|---|---|---|---|
| `AppButton` `sm` | 32px | 12px | 8px | 1px |
| `AppButton` `md` | 36px | 14px | 8px | 1px |
| `AppButton` `list` | 行高跟随列表 | 左右 12px | 8px | 默认透明，激活时 1px |

默认按钮文案字号按 14px 理解。

### 图标按钮

图标按钮是 Foliole 很重要的一类桌面控件。

它们应该看起来：

- 紧凑
- 易扫读
- 有轻反馈
- 不发亮、不发飘

激活态应表达“已选中”，而不是“很兴奋”。

当前图标按钮的默认规格：

| 项目 | 默认值 |
|---|---|
| 尺寸 | 32px × 32px |
| 圆角 | 8px |
| 图标常见尺寸 | 16px |
| 默认边框 | 1px 透明边框 |

标题栏里的更小图标按钮可以收缩到 30px，极窄 rail 里的单元宽度以 40px 栅格对齐。

### 工具条

工具条的职责是给动作分组和定结构，不是做视觉表演。

- 高度应低而稳
- 分组关系要清楚
- 只有真正需要构成区域时，才给出明确背景

默认工具条高度按 40px 理解；标题栏系统 chrome 按 38px 理解。

### 面板与检查器

面板应该是安静的容器。

- 优先使用平面表面、边界和轻对比
- 标题栏负责说明结构，不负责装饰
- 检查器区域应比正文区更密、更工具化

检查器标题区应偏小而明确：

- 上层标签约 11px，全大写可接受
- 主标题约 14px
- 内容区默认使用 12px 到 14px 的信息密度

### 输入框与表单控件

输入控件应给人稳定、实用的感觉。

- 背景偏浅
- 边界清楚
- 圆角适中
- 聚焦态明显但克制

设置项中的控件应对齐整齐，并共享相近的尺寸感。

当前输入框默认规格：

| 项目 | 默认值 |
|---|---|
| 高度 | 40px |
| 左右内边距 | 12px |
| 上下内边距 | 4px |
| 边框 | 1px |
| 圆角 | 默认按 8px 落地 |
| 字号 | 16px |

设置项控制区默认宽度可按 360px 上限理解。

### 菜单、对话框与提示层

所有浮层都应属于同一个家族：

- 浅色抬升表面
- 统一边框
- 统一圆角
- 统一柔和阴影
- 不做毛玻璃
- 不做夸张模糊和厚重漂浮感

当前浮层默认规格：

| 类型 | 圆角 | 阴影 | 常见偏移 / 内边距 |
|---|---|---|---|
| 菜单 / 下拉 | 12px | `shadow-popover` | side offset 4px，内容内边距 4px |
| Tooltip | 12px | `shadow-popover` | side offset 6px，内容 `10px 6px` 左右 |
| Dialog | 12px 基线 | `shadow-panel` | 居中浮起 |

Tailwind 里当前阴影令牌为：

- `shadow-popover`: `0 18px 40px rgba(15, 17, 19, 0.12)`
- `shadow-panel`: `0 10px 24px rgba(15, 17, 19, 0.08)`

### 无壳极简表面

轻量输入、反馈和状态提示类表面默认采用无壳极简方向。

这里的“无壳”不是没有边界，而是不把表面做成传统 `header / body / footer` 三段式 chrome，也不是把 header 删掉后留下一个大输入框和硬 footer。它应该首先像一张单一的悬浮纸面：靠柔和阴影、极弱边界和留白定位，内容和操作同属一个轻表面。

适用表面：

- Quick Capture
- 发送反馈
- 轻量状态通知
- 其他临时输入或补充信息表面

规则：

- 不默认放大标题栏；标题能由 placeholder 或主体文案承担时，标题退场。
- 字段标签优先退成占位符式弱文本，例如“称谓，可不填”“联系方式，可不填”。
- 元信息应贴近底部操作区，放在底部分割线上方；不要混进主输入正文区。
- 底部操作区可以有一条极弱顶部分割线，但不默认使用底部色块；这条线只能像纸面上的轻折痕，不能像传统 dialog footer。
- 强调顺序是留白、文字层级、极弱线条；不到必要时不新增背景色块。
- 外边框只能帮助 surface 从文档中浮起，不能强到像表单控件边框。
- 透明宿主窗口必须给阴影留出 gutter，不能让 surface 铺满窗口后把阴影裁掉。
- 同一家族表面的外圆角应统一；内部按钮圆角小于外层圆角。
- Save / Send 不默认使用强色或品牌色填充，优先轻描边或文字动作，文字强度也应低于正文正黑。
- 品牌 leaf 不作为默认操作提示装饰；只在品牌识别或成功状态里低存在感出现。
- Toast 标题不使用接近正文正黑的强文字，除非它承担正文内容本身。

### 标签与状态块

状态标签的职责是传达信息，不是装饰。

- 默认优先中性
- 只有语义明确时才使用状态色
- 尺寸保持紧凑和清晰

### 空状态

空状态应该短、清楚、低情绪。

不要放插画，不要拟人，不要过度鼓励式文案。

## 5. 间距体系

Foliole 需要一套显式可复用的间距刻度，避免 AI 每次自由发挥。

默认使用下面这组节奏：

| 令牌级别 | 数值 | 典型用途 |
|---|---|---|
| `space-1` | 4px | 极小分隔、内联微间距 |
| `space-2` | 8px | 按钮内小间距、小型块间距 |
| `space-3` | 12px | 表单和小面板的常规内边距 |
| `space-4` | 16px | 面板主体常规 padding、列表区稳定间距 |
| `space-5` | 20px | 中等区块缓冲 |
| `space-6` | 24px | 大 section 间距、浮层内容缓冲 |
| `space-8` | 32px | 大块区域留白 |

优先规则：

- 小控件之间优先用 4px / 6px / 8px
- 工具条和紧凑分组优先用 6px 到 12px
- 面板主体 padding 优先用 12px 到 16px
- 大 section 间距优先用 24px 到 32px

没有明确理由时，不要跳出这套节奏。

## 6. 状态定义

### 交互状态基线

AI 在落地控件时，不能只写“有 hover / active / disabled”，需要按下面这组强度理解：

| 状态 | 默认表现 |
|---|---|
| 默认 | 中性表面、中性文字、边界清楚 |
| `hover` | 轻度加深前景色，背景增加约 4% 前景透明度 |
| `active/selected` | 比 hover 再强一档，背景约 5% 到 8% 前景透明度，必要时补强边界 |
| `focus-visible` | 1px ring 或更强边界，使用 `ring` / `border-strong` 体系 |
| `disabled` | 降低到约 45% 到 55% 不透明度，并禁用指针语义 |
| `muted` | 降低存在感，但仍需保证可读 |

### 当前共享控件状态锚点

当前仓库里最值得直接复用的状态强度包括：

- 按钮 / 图标按钮 hover：`bg-foreground/[0.04]`
- 按钮 / 图标按钮 active：`bg-foreground/[0.05]` 到 `bg-foreground/[0.08]`
- 文字 hover：从 `text-foreground/70` 提升到 `text-foreground`
- 输入框 focus：`focus-visible:ring-1 focus-visible:ring-border-strong`
- disabled：常见为 `opacity-45` 到 `opacity-50`
- 树节点弱化态：约 `opacity: 0.35`

如果做新控件，优先贴近这组强度，不要另发明一套更花的交互反馈。

## 7. 颜色与变量映射

为了让 AI 在仓库里更容易落地，这里补一层“设计角色 <-> 当前实现”的对照。

### Tailwind 颜色令牌

| 设计角色 | Tailwind 令牌 |
|---|---|
| 主背景 | `background` |
| 主文字 | `foreground` |
| 面板背景 | `bg-panel` |
| 浮层背景 | `bg-elevated` |
| 轻表面 | `bg-subtle` |
| 默认边界 | `border` |
| 强边界 | `border-strong` |
| 中性强调 | `accent` / `accent-strong` |
| 焦点 ring | `ring` |

### CSS 变量映射

| 设计角色 | 当前变量名 |
|---|---|
| 品牌/确认绿色 | `--app-accent-color` |
| 选中主色 | `--app-selection-color` |
| 选中表面色 | `--app-selection-surface-color` |
| 高亮主色 | `--app-highlight-color` |
| 高亮表面色 | `--app-highlight-surface-color` |
| 挖空主色 | `--app-cloze-color` |
| 挖空表面色 | `--app-cloze-surface-color` |
| 界面字体 | `--font-family-interface` / `--app-interface-font-family` |
| 正文字体 | `--font-family-text` / `--content-panel-font-family` |
| 等宽字体 | `--font-family-mono` / `--content-panel-mono-font-family` |
| 正文字号 | `--content-panel-font-size` |
| 正文 H1 | `--content-panel-h1-font-size` |
| 正文 H2 | `--content-panel-h2-font-size` |
| 正文 H3 | `--content-panel-h3-font-size` |
| 正文代码字号 | `--content-panel-code-font-size` |
| 左侧列表宽度 | `--workspace-list-width` |
| 右侧检查器宽度 | `--workspace-right-sidebar-width` |
| 正文最大宽度 | `--document-max-width` |

新增视觉实现时，优先使用已有 Tailwind 令牌；只有当前令牌无法表达时，才考虑补 token，而不是直接写死值。

## 8. 布局原则

### 主工作台骨架

Foliole 的核心壳层是一个三段式桌面工作区：

- 左侧导航区
- 中间文档/工作区
- 右侧检查器区

另外还有一条窄左 rail 作为工作台级入口锚点。

标题栏和内容列宽之间应保持耦合感，让人觉得它们属于同一个窗口系统。

### 结构表达方式

主壳层应主要通过这些手段建立：

- 列与列的关系
- 分隔线
- 固定锚点
- 可预测的间距

而不是：

- 到处叠卡片边框
- 一层套一层装饰盒子
- 每一块都有不同的底色

### 正文区域

中间文档区是整个产品最需要呼吸感的地方。

它应该是：

- 最亮的
- 最易读的
- 最安静的
- 最内容优先的

它可以比侧栏更松一点，但仍然要和整体工作台属于同一个系统。

### 密度模型

不同区域应允许不同密度：

- 导航区可以更密一点
- 检查器区可以更紧、更信息化
- 设置页应中等密度、整洁
- 阅读和编辑区应给更多呼吸空间

不要把同一套间距节奏强行压到所有区域上。

### 间距逻辑

优先追求：

- 小而稳定的间距体系
- 清楚的分组节奏
- 靠对齐表达秩序，而不是靠装饰

避免：

- 为了“高级感”而把 padding 做得很大
- 把控件压得太紧导致不安心
- 同类结构之间间距不一致

## 9. 深度、阴影与圆角

### 表面层级

Foliole 整体应偏平，不靠强烈立体感成立。

默认层级关系应是：

1. 应用背景
2. 面板表面
3. 浮层表面

不要把阴影和悬浮感当成主要层次工具。

### 阴影模型

阴影只保留给真正有抬升语义的层：

- 主壳层大多数区域不需要明显阴影
- 菜单和对话框可以有柔和阴影
- 避免阴影叠阴影

### 圆角模型

圆角应当克制、实用、偏桌面工具。

沿用当前已成型的半径体系：

- `sm`: 4px
- `md`: 8px
- `lg`: 12px
- `xl`: 16px

默认使用应主要落在 `md` 到 `lg` 之间。

避免：

- 全局太硬、全零圆角
- 全局太软、像移动端卡片

## 10. 应该做与不该做

### 应该做

- 从桌面工作台骨架开始设计，而不是从局部卡片开始
- 默认使用中性表面
- 让边框、间距和结构承担主要层次表达
- 保持控件紧凑且可重复
- 让编辑和阅读区比工具区更安静
- 让对比关系明确但不过激
- 优先复用共享组件和共享外观
- 让检查器和设置页显得系统化

### 不该做

- 把产品做成营销网站味
- 把渐变、光效、电影感阴影变成默认语言
- 把绿色用成整套界面的主氛围色
- 把整个工作台铺满卡片
- 靠大面积染色强行制造层次
- 把按钮做得膨胀、柔软、像玩具
- 把第三方现成视觉语言整套塞进核心工作区
- 以暗黑炫酷工具气质作为唯一中心

## 11. 响应式行为

### 前提

Foliole 是桌面优先产品。响应式的目标是维持可用性，不是把它改造成移动端式应用。

### 窄宽度时

在较窄宽度下：

- 左 rail 可以隐藏
- 主区域可以改成堆叠
- 拖拽把手和桌面并排结构可以收缩
- 核心阅读和编辑体验必须保住

### 必须保住的东西

响应式变化必须保住：

- 导航可理解性
- 编辑连续性
- 关键操作入口
- 可读的内容宽度

### 不应该发生的事

- 不要默认变成很大的触屏式控件
- 不要改造成卡片流
- 不要丢掉桌面工具身份

## 12. 给 AI 的简明提示

### 一句话摘要

如果你在生成 Foliole 的界面，请按下面这句话理解：

“这是一个桌面优先的知识工作台，整体以安静的灰白中性色为主，绿色只做少量强调。界面依赖精确分隔线、紧凑桌面控件和清楚的三段式工作区结构。中间阅读与编辑区应最安静、最内容优先；侧栏和检查器应更紧凑、更工具化。避免营销网站风格、夸张渐变、满屏卡片、戏剧性阴影和装饰性大色块。”

### 参考对象

以下对象只可作为行为参考，不能直接照着仿：

- Obsidian：参考桌面工作区姿态和长期使用感
- Linear：参考结构秩序和布局克制感
- Raycast：参考紧凑桌面控件的利落感
- Notion：参考内容区的安静可读性

不能直接复制任何一个产品的皮。

### 当前实现锚点

如果拿不准，应优先和仓库里现有共享组件、工作台主壳、设置页模式保持一致。

### 未来暗色模式说明

这份文档目前不把暗色模式作为完整定稿目标。

如果以后要补暗色模式，应继续保持：

- 同样安静的结构
- 同样克制的颜色逻辑
- 同样桌面优先的主壳关系

不能变成霓虹、发光、过高对比的默认风格。

## 13. Foliole 特有界面说明

### 窗口标题栏

标题栏是产品主壳的一部分，不是随手加上的顶部条。

它应该显得：

- 薄
- 和工作区列宽有对齐关系
- 低噪音
- 有系统窗口意识

### 左侧 rail

左侧 rail 是工作台的紧凑锚点。

它应该非常简单、稳定、可重复。它的职责是定方向和快速切换，不是装饰。

### 节点列表

节点列表是高密度导航区。

它应优先保证：

- 扫读速度
- 缩进和层级稳定
- 强调适中
- 当前态和弱化态清楚

### 文档头部

文档头部要帮助用户定位和切换，但不能从内容本身手里抢焦点。

### 复习工具条

复习工具条是高频动作面。

它可以比普通工具条有更明确一点的动作感，但依然要待在整套克制体系里。

### 设置页

设置页应该特别系统化。

它是最适合体现一致性的地方之一：

- section 节奏
- 标签语气
- 控件对齐
- 说明文字长度

设置页应该给人可靠、整洁、容易扫读的感觉，不能像试验场。

### 独立桌面浮层

由 Electron 主进程创建、显示在应用窗口外的轻浮层，例如全局剪辑提示、全局捕捉输入框，也属于 Foliole 界面，不是系统通知或临时网页。

这类浮层必须：

- 读取当前应用窗口的 floating surface token，而不是只跟随操作系统深浅色
- 复用同一套背景、边框、阴影、圆角、文字和品牌标记节奏
- 只把状态、输入区域、点击能力作为差异，不各自复制一套视觉体系
- 失败或兜底时也保持低噪音，不出现和当前应用模式相反的强边框或强底色
- 键盘提示要低存在感，不能抢走主要输入或结果标题的层级

## 14. 当前与目标之间的差口

这份文档描述的是 Foliole 的目标设计基线。当前产品整体方向已经接近，但还没有完全收齐。

目前已知、后续应逐步收口的地方包括：

- 有些工作区表面还在使用硬编码颜色，没有完全回收到共享语义色里
- 有些面包屑和元信息文字颜色还是局部定义，没有统一到共享文字角色
- 有些检查器和统计块用了更具体的染色背景，比长期目标更“局部”
- 有些对话框和工具面板的圆角比默认节奏更大
- 有些 feature 层控件还在局部重写共享样式，而不是先补共享组件

这些现状不代表以后应该继续照着扩散。

后续新增界面或重做旧界面时，应优先向这份文档靠拢，而不是保留局部不一致。
