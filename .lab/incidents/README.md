# Incident Files

- Store incident reports, postmortems, root-cause investigations, and temporary recovery plans here.
- Incident files are not agent runtime state and do not belong in `.lab/agent/`.
- If an incident leads to a stable platform rule, extract that rule into `.lab/specs/**` or `AGENTS.md`.
